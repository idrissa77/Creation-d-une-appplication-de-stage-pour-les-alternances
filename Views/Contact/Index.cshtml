@model IEnumerable<stages.Models.Contact>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DatecontactNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdfetudiantNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NopropositionNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DatecontactNavigation.Datecontact)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdfetudiantNavigation.Idfetudiant)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NopropositionNavigation.Noproposition)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
