@model IEnumerable<stages.Models.Enseignant>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nomenseignant)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Prenomenseignant)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Datevisite)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nomenseignant)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prenomenseignant)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Datevisite)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idfenseignant">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idfenseignant">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idfenseignant">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
